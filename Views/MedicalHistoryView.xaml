<UserControl x:Class="ClinicAppointmentSystem.Views.MedicalHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClinicAppointmentSystem.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:ClinicAppointmentSystem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.DataContext>
        <vm:MedicalHistoryViewModel />
    </UserControl.DataContext>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="*" />    <!-- Timeline -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,40">
            <TextBlock Text="MEDICAL HISTORY" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" />
            <TextBlock Text="A complete chronological record of your past clinical consultations and diagnoses." Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Opacity="0.6" Margin="0,8,0,0" />
        </StackPanel>

        <!-- 2. TIMELINE FEED -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20,0,0,0">
                <!-- Vertical Timeline Line -->
                <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="2,0,0,0" HorizontalAlignment="Left" Margin="14,0,0,0" />
                
                <ItemsControl ItemsSource="{Binding Records}" Margin="0,10,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,40">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Bullet -->
                                <materialDesign:Card Grid.Column="0" UniformCornerRadius="16" Width="32" Height="32" Background="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Top" Margin="0,5,30,0">
                                    <materialDesign:PackIcon Kind="RecordCircle" Foreground="White" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </materialDesign:Card>

                                <!-- Content -->
                                <materialDesign:Card Grid.Column="1" UniformCornerRadius="12" Padding="25" materialDesign:ElevationAssist.Elevation="Dp2" MaxWidth="800" HorizontalAlignment="Left">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Date, StringFormat='dd MMMM yyyy'}" Style="{StaticResource MaterialDesignSubtitle2TextBlock}" FontWeight="Bold" Foreground="#3B82F6" />
                                        <TextBlock Text="{Binding Title}" Style="{StaticResource MaterialDesignHeadline6TextBlock}" FontWeight="Bold" Margin="0,10,0,5" />
                                        <TextBlock Text="{Binding Treatment}" Style="{StaticResource MaterialDesignBody1TextBlock}" Opacity="0.7" TextWrapping="Wrap" LineHeight="20" />
                                        
                                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0" Margin="0,15,0,10" />
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="HospitalBuilding" Foreground="#3B82F6" Opacity="0.5" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <TextBlock Text="Clinical Center | Alpha Medical Group" Opacity="0.5" VerticalAlignment="Center" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </StackPanel>
                                </materialDesign:Card>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
