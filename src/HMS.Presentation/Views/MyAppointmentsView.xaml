<UserControl x:Class="HMS.Core.Views.MyAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:HMS.Core.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="#F8FAFC">

    <UserControl.DataContext>
        <vm:MyAppointmentsViewModel />
    </UserControl.DataContext>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="Auto" /> <!-- Search & Stats -->
            <RowDefinition Height="*" />    <!-- Main Content -->
        </Grid.RowDefinitions>
 
        <!-- 1. HEADER SECTION -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="MY MEDICAL VISITS" 
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                       FontWeight="Black" Foreground="#3B82F6" />
            <TextBlock Text="Review your upcoming consultations and clinical history records." 
                       Style="{StaticResource MaterialDesignBody1TextBlock}" 
                       Opacity="0.5" Margin="0,8,0,0" />
        </StackPanel>
 
        <!-- 2. SEARCH & STATS -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <materialDesign:Card UniformCornerRadius="16" Padding="15,5" materialDesign:ElevationAssist.Elevation="Dp1" Background="White">
                <Grid>
                    <materialDesign:PackIcon Kind="Magnify" Foreground="#64748B" Width="22" Height="22" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             materialDesign:HintAssist.Hint="Filter by physician or reason..." 
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             BorderThickness="0" Margin="45,0,10,0" FontSize="14" />
                </Grid>
            </materialDesign:Card>
 
            <Button Grid.Column="1" Style="{StaticResource MaterialDesignRaisedButton}" 
                    Background="#3B82F6" BorderBrush="#3B82F6"
                    materialDesign:ButtonAssist.CornerRadius="12" Margin="15,0,0,0" Height="50" Padding="25,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="NEW APPOINTMENT" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>
 
        <!-- 3. MAIN CONTENT: TABS -->
        <TabControl Grid.Row="2" Margin="0,20,0,0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource MaterialDesignTabItem}">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="20,10"/>
                </Style>
            </TabControl.Resources>
            
            <!-- UPCOMING -->
            <TabItem Header="UPCOMING">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding UpcomingAppointments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card UniformCornerRadius="20" Margin="0,0,15,20" Padding="0" materialDesign:ElevationAssist.Elevation="Dp2" Background="White">
                                    <Grid Height="120">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Date Indicator -->
                                        <Border Background="#EFF6FF" Width="100" CornerRadius="20,0,0,20">
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding AppointmentDate, StringFormat='dd'}" FontSize="28" FontWeight="Black" Foreground="#3B82F6" HorizontalAlignment="Center" />
                                                <TextBlock Text="{Binding AppointmentDate, StringFormat='MMM'}" FontSize="12" FontWeight="Bold" Foreground="#60A5FA" HorizontalAlignment="Center" Margin="0,-2,0,0" />
                                            </StackPanel>
                                        </Border>
                                        
                                        <!-- Doctor Info -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="30,0">
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                <materialDesign:PackIcon Kind="Doctor" Foreground="#3B82F6" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding DoctorName}" FontWeight="Bold" FontSize="16" Foreground="#1E293B" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding DoctorSpecialty}" Foreground="#64748B" FontSize="13" FontWeight="SemiBold" />
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                <materialDesign:PackIcon Kind="ClockOutline" Width="16" Height="16" Foreground="#94A3B8" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding AppointmentDate, StringFormat='hh:mm tt'}" Foreground="#94A3B8" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <!-- Status & Action -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,30,0">
                                            <Border Background="#DBEAFE" CornerRadius="10" Padding="12,5" Margin="0,0,0,15" HorizontalAlignment="Right">
                                                <TextBlock Text="CONFIRMED" Foreground="#1D4ED8" FontWeight="Black" FontSize="10" />
                                            </Border>
                                            <Button Content="DETAILS" Background="#EFF6FF" Foreground="#3B82F6" BorderBrush="#3B82F6" 
                                                    Style="{StaticResource MaterialDesignOutlinedButton}" Height="34" Padding="20,0" materialDesign:ButtonAssist.CornerRadius="10" />
                                        </StackPanel>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
 
            <!-- PAST -->
            <TabItem Header="PAST VISITS">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding PastAppointments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card UniformCornerRadius="16" Margin="0,0,15,12" Padding="20" materialDesign:ElevationAssist.Elevation="Dp1" Background="White">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <Border Width="40" Height="40" Background="#F8FAFC" CornerRadius="10">
                                            <materialDesign:PackIcon Kind="CheckCircle" Foreground="#10B981" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <StackPanel Grid.Column="1" Margin="20,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DoctorName}" FontWeight="Bold" FontSize="14" Foreground="#1E293B" />
                                            <TextBlock Text="{Binding AppointmentDate, StringFormat='dddd, MMMM dd yyyy'}" Foreground="#64748B" FontSize="12" />
                                        </StackPanel>
 
                                        <Border Grid.Column="2" Background="#ECFDF5" CornerRadius="8" Padding="10,4" VerticalAlignment="Center">
                                            <TextBlock Text="COMPLETED" Foreground="#059669" FontWeight="Black" FontSize="10" />
                                        </Border>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
 
            <!-- CANCELLED -->
            <TabItem Header="CANCELLED">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                    <ItemsControl ItemsSource="{Binding CancelledAppointments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card UniformCornerRadius="16" Margin="0,0,15,12" Padding="20" Background="#FFF1F2" materialDesign:ElevationAssist.Elevation="Dp0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <materialDesign:PackIcon Kind="CloseCircle" Foreground="#EF4444" Width="24" Height="24" VerticalAlignment="Center" Margin="5,0,20,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding AppointmentDate, StringFormat='MMMM dd, yyyy'}" FontWeight="Bold" Foreground="#991B1B" />
                                            <TextBlock Text="Session Cancelled" Foreground="#EF4444" Opacity="0.7" FontSize="12" />
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="REBOOK" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3B82F6" FontWeight="Black" VerticalAlignment="Center" />
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
