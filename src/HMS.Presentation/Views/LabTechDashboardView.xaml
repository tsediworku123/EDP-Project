<UserControl x:Class="HMS.Core.Views.LabTechDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <!-- Stats Cards Overview -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Pending Card -->
                <materialDesign:Card Grid.Column="0" Style="{StaticResource PremiumCard}" Margin="0,0,15,0" Background="White">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="PENDING QUEUE" Foreground="#64748B" FontSize="13" FontWeight="Bold" />
                            <TextBlock Text="{Binding PendingTestsCount}" Foreground="#0F172A" FontSize="36" FontWeight="ExtraBold" Margin="0,10,0,0" />
                            <TextBlock Text="Tests awaiting action" Foreground="#94A3B8" FontSize="12" Margin="0,2,0,0" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="#EEF2FF" CornerRadius="12" Width="50" Height="50" VerticalAlignment="Top">
                            <materialDesign:PackIcon Kind="ClockOutline" Foreground="#4F46E5" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </materialDesign:Card>

                <!-- Completed Card -->
                <materialDesign:Card Grid.Column="1" Style="{StaticResource PremiumCard}" Margin="15,0" Background="White">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="COMPLETED TODAY" Foreground="#64748B" FontSize="13" FontWeight="Bold" />
                            <TextBlock Text="{Binding CompletedTodayCount}" Foreground="#0F172A" FontSize="36" FontWeight="ExtraBold" Margin="0,10,0,0" />
                            <TextBlock Text="Total finalized reports" Foreground="#94A3B8" FontSize="12" Margin="0,2,0,0" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="#ECFDF5" CornerRadius="12" Width="50" Height="50" VerticalAlignment="Top">
                            <materialDesign:PackIcon Kind="CheckCircleOutline" Foreground="#10B981" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </materialDesign:Card>

                <!-- Total Tests Card -->
                <materialDesign:Card Grid.Column="2" Style="{StaticResource PremiumCard}" Margin="15,0,0,0" Background="White">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="TOTAL VOLUME" Foreground="#64748B" FontSize="13" FontWeight="Bold" />
                            <TextBlock Text="{Binding TotalTestsCount}" Foreground="#0F172A" FontSize="36" FontWeight="ExtraBold" Margin="0,10,0,0" />
                            <TextBlock Text="All registered lab tests" Foreground="#94A3B8" FontSize="12" Margin="0,2,0,0" />
                        </StackPanel>
                        <Border Grid.Column="1" Background="#EFF6FF" CornerRadius="12" Width="50" Height="50" VerticalAlignment="Top">
                            <materialDesign:PackIcon Kind="FlaskOutline" Foreground="#3B82F6" Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </materialDesign:Card>
            </Grid>

            <!-- Activity / Recent List -->
            <materialDesign:Card Style="{StaticResource PremiumCard}" Margin="0,30,0,0" Padding="0">
                <StackPanel>
                    <Border Background="#F8FAFC" Padding="25,20" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" CornerRadius="12,12,0,0">
                        <Grid>
                            <TextBlock Text="RECENT TEST ACTIVITY" FontWeight="ExtraBold" Foreground="#334155" FontSize="15" VerticalAlignment="Center" />
                            <Button HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3B82F6" Content="SEE ALL" Height="24" Padding="10,0" />
                        </Grid>
                    </Border>
                    
                    <DataGrid ItemsSource="{Binding RecentTests}" AutoGenerateColumns="False" CanUserAddRows="False"
                              IsReadOnly="True" Background="Transparent" BorderThickness="0"
                              SelectionUnit="FullRow" SelectionMode="Single"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9"
                              RowHeight="65" BorderBrush="Transparent" Margin="0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="White" />
                                <Setter Property="Foreground" Value="#94A3B8" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="FontSize" Value="12" />
                                <Setter Property="Padding" Value="25,15" />
                                <Setter Property="BorderThickness" Value="0,0,0,2" />
                                <Setter Property="BorderBrush" Value="#F1F5F9" />
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="25,0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                                            <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        
                        <DataGrid.Columns>
                            <!-- Patient Name -->
                            <DataGridTemplateColumn Header="PATIENT" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" Margin="25,0,0,0">
                                            <TextBlock Text="{Binding PatientName}" FontWeight="Bold" FontSize="15" Foreground="#1E293B" />
                                            <TextBlock Text="{Binding PatientId, StringFormat='ID: {0}'}" FontSize="12" Foreground="#64748B" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Test Name -->
                            <DataGridTemplateColumn Header="TEST NAME" Width="2.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding TestName}" FontWeight="Bold" FontSize="15" Foreground="#1E293B" />
                                            <TextBlock Text="{Binding TestCategory}" FontSize="12" Foreground="#64748B" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Date Requested -->
                            <DataGridTemplateColumn Header="DATE REQUESTED" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding RequestedDate, StringFormat='{}{0:MMM dd, yyyy}'}" FontSize="14" Foreground="#334155" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding RequestedDate, StringFormat='{}{0:hh:mm tt}'}" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Status Badge -->
                            <DataGridTemplateColumn Header="CURRENT STATUS" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="6" Padding="12,6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                            <Setter Property="Background" Value="#DCFCE7" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Requested">
                                                            <Setter Property="Background" Value="#FEE2E2" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="Sample Collected">
                                                            <Setter Property="Background" Value="#E0E7FF" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="In Progress">
                                                            <Setter Property="Background" Value="#FEF3C7" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Foreground" Value="#166534" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Requested">
                                                                <Setter Property="Foreground" Value="#991B1B" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Sample Collected">
                                                                <Setter Property="Foreground" Value="#3730A3" />
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="In Progress">
                                                                <Setter Property="Foreground" Value="#92400E" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
