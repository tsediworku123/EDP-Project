<UserControl x:Class="HMS.Core.Views.AdminAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="MASTER APPOINTMENT LOG" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="#1E293B" />
            <TextBlock Text="Full overview of all clinical visits and scheduling status." Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Opacity="0.6" />
        </StackPanel>

        <materialDesign:Card Grid.Row="1" UniformCornerRadius="12" materialDesign:ElevationAssist.Elevation="Dp1">
            <DataGrid ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="DATE" Binding="{Binding AppointmentDate, StringFormat=dd MMM yyyy HH:mm}" Width="150" />
                    <DataGridTextColumn Header="PATIENT ID" Binding="{Binding PatientId}" Width="100" />
                    <DataGridTextColumn Header="DOCTOR ID" Binding="{Binding DoctorId}" Width="100" />
                    <DataGridTextColumn Header="REASON" Binding="{Binding Reason}" Width="*" />
                    <DataGridTemplateColumn Header="STATUS" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F1F5F9" CornerRadius="4" Padding="8,4">
                                    <TextBlock Text="{Binding Status}" FontWeight="Bold" FontSize="11" HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
