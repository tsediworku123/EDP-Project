<Window x:Class="HMS.Core.Views.Auth.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Register New Account" Height="650" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="16" Background="#0F172A" Effect="{DynamicResource MaterialDesignShadowDepth5}">
        <Grid>
            <!-- Drag handle -->
            <Border Height="40" VerticalAlignment="Top" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Cursor="SizeAll"/>
            <!-- Close button -->
            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,12,0" Width="32" Height="32"
                    Style="{StaticResource MaterialDesignIconButton}" Foreground="#94A3B8" Click="CloseButton_Click" ToolTip="Close">
                <materialDesign:PackIcon Kind="Close" Width="18" Height="18"/>
            </Button>

            <StackPanel VerticalAlignment="Center" Margin="40">
                <TextBlock Text="Create Account" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Join MediCare HMS as a new Patient" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,4,0,30"/>

                <TextBlock Text="FULL NAME" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,0,0,6"/>
                <Border CornerRadius="8" Background="#1E293B" Margin="0,0,0,15" Height="45">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="AccountOutline" Foreground="#3B82F6" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" x:Name="NameBox" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="14" VerticalAlignment="Center" Padding="0,0,12,0"/>
                    </Grid>
                </Border>

                <TextBlock Text="EMAIL ADDRESS" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,0,0,6"/>
                <Border CornerRadius="8" Background="#1E293B" Margin="0,0,0,15" Height="45">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="EmailOutline" Foreground="#3B82F6" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" x:Name="EmailBox" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="14" VerticalAlignment="Center" Padding="0,0,12,0"/>
                    </Grid>
                </Border>
                
                <TextBlock Text="PHONE NUMBER" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,0,0,6"/>
                <Border CornerRadius="8" Background="#1E293B" Margin="0,0,0,15" Height="45">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="PhoneOutline" Foreground="#3B82F6" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" x:Name="PhoneBox" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="14" VerticalAlignment="Center" Padding="0,0,12,0"/>
                    </Grid>
                </Border>

                <TextBlock Text="PASSWORD" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,0,0,6"/>
                <Border CornerRadius="8" Background="#1E293B" Margin="0,0,0,15" Height="45">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="LockOutline" Foreground="#3B82F6" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" x:Name="PasswordBox" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="14" VerticalAlignment="Center" Padding="0,0,12,0"/>
                    </Grid>
                </Border>

                <TextBlock Text="CONFIRM PASSWORD" FontSize="11" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,0,0,6"/>
                <Border CornerRadius="8" Background="#1E293B" Margin="0,0,0,30" Height="45">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="LockCheckOutline" Foreground="#3B82F6" Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" x:Name="ConfirmPasswordBox" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="14" VerticalAlignment="Center" Padding="0,0,12,0"/>
                    </Grid>
                </Border>

                <Button Height="50" Click="RegisterButton_Click" Cursor="Hand" Background="#3B82F6" Foreground="White" materialDesign:ButtonAssist.CornerRadius="8" BorderThickness="0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountPlusOutline" Width="18" Height="18" VerticalAlignment="Center"/>
                        <TextBlock Text="Register Now" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Already have an account?" Foreground="#64748B" VerticalAlignment="Center"/>
                    <Button Content="Sign In" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3B82F6" FontWeight="Bold" Height="30" Click="BackToLogin_Click" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
