<UserControl x:Class="HMS.Core.Views.BillingInvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header & Search -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="350" />
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="INVOICES &amp; BILLING" FontSize="22" FontWeight="ExtraBold" Foreground="#0F172A"/>
                <TextBlock Text="Manage patient billing records and generate new invoices." FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Search Invoice # or Patient ID..."
                         materialDesign:TextFieldAssist.HasClearButton="True"
                         Background="White" materialDesign:ButtonAssist.CornerRadius="12"/>
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding CreateInvoiceCommand}" Margin="15,0,0,0"
                        Background="#10B981" BorderThickness="0" Height="45" Padding="20,0" materialDesign:ButtonAssist.CornerRadius="12">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0"/>
                        <TextBlock Text="NEW INVOICE" FontWeight="Bold"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>

        <!-- Invoices List -->
        <materialDesign:Card Grid.Row="1" Padding="0" Style="{StaticResource PremiumCard}" Background="White">
            <DataGrid ItemsSource="{Binding Invoices}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0" HeadersVisibility="Column"
                      RowHeight="60">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="INVOICE #" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding InvoiceNumber}" VerticalAlignment="Center" Margin="20,0" FontWeight="Bold" Foreground="#1E293B"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="PATIENT ID" Binding="{Binding PatientId}" Width="1*" />
                    
                    <DataGridTemplateColumn Header="DATE" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding BillDate, StringFormat='{}{0:MMM dd, yyyy}'}" VerticalAlignment="Center" Foreground="#64748B"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="TOTAL" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:C}'}" VerticalAlignment="Center" FontWeight="Bold" Foreground="#0F172A"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="STATUS" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="12,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                    <Setter Property="Background" Value="#DCFCE7" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Unpaid">
                                                    <Setter Property="Background" Value="#FEE2E2" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Partially Paid">
                                                    <Setter Property="Background" Value="#FEF3C7" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Paid">
                                                        <Setter Property="Foreground" Value="#166534" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Unpaid">
                                                        <Setter Property="Foreground" Value="#991B1B" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Partially Paid">
                                                        <Setter Property="Foreground" Value="#92400E" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="ACTIONS" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Foreground="#3B82F6"
                                            ToolTip="View Details" Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="Eye" Width="18" Height="18"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Foreground="#10B981"
                                            ToolTip="Process Payment">
                                        <materialDesign:PackIcon Kind="CreditCardCheck" Width="18" Height="18"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Foreground="#64748B"
                                            ToolTip="Print Invoice">
                                        <materialDesign:PackIcon Kind="Printer" Width="18" Height="18"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
