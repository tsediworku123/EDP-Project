<UserControl x:Class="HMS.Core.Views.ProcessPaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">

    <Border Background="White" CornerRadius="20" Padding="30" Width="400">
        <StackPanel>
            <TextBlock Text="PROCESS PAYMENT" FontSize="18" FontWeight="Black" Foreground="#0F172A" Margin="0,0,0,25"/>

            <materialDesign:Card Background="#F8FAFC" Padding="20" Margin="0,0,0,25">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="REMAINING BALANCE" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                    <TextBlock Grid.Row="1" Text="{Binding RemainingBalance, StringFormat='{}{0:C}'}" FontSize="28" FontWeight="Black" Foreground="#1E293B" Margin="0,5,0,0"/>
                </Grid>
            </materialDesign:Card>

            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Payment Amount" FontWeight="Bold" Margin="0,0,0,8" FontSize="13"/>
                <TextBox Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:TextFieldAssist.PrefixText="$"
                         materialDesign:ButtonAssist.CornerRadius="10"/>
            </StackPanel>

            <StackPanel Margin="0,0,0,30">
                <TextBlock Text="Payment Method" FontWeight="Bold" Margin="0,0,0,8" FontSize="13"/>
                <ComboBox SelectedValue="{Binding PaymentMethod}" SelectedValuePath="Content"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:ButtonAssist.CornerRadius="10">
                    <ComboBoxItem Content="Cash" IsSelected="True"/>
                    <ComboBoxItem Content="Credit Card"/>
                    <ComboBoxItem Content="Bank Transfer"/>
                    <ComboBoxItem Content="Digital Wallet"/>
                </ComboBox>
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Style="{StaticResource MaterialDesignFlatButton}" Content="CANCEL" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Margin="0,0,10,0" Height="45" materialDesign:ButtonAssist.CornerRadius="12"/>
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignRaisedButton}" Content="CONFIRM" Command="{Binding ConfirmPaymentCommand}" Background="#10B981" BorderThickness="0" Height="45" materialDesign:ButtonAssist.CornerRadius="12"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
