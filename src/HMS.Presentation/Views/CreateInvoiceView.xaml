<UserControl x:Class="HMS.Core.Views.CreateInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Border Background="White" CornerRadius="20" Padding="30" Width="750">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="CREATE NEW INVOICE" FontSize="20" FontWeight="Black" Foreground="#0F172A" Margin="0,0,0,25"/>

            <!-- Patient Selection -->
            <StackPanel Grid.Row="1" Margin="0,0,0,25">
                <TextBlock Text="Select Patient" FontWeight="Bold" Margin="0,0,0,8" FontSize="13"/>
                <ComboBox ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          materialDesign:HintAssist.Hint="Search for a patient..."
                          DisplayMemberPath="FullName"
                          materialDesign:ButtonAssist.CornerRadius="10"/>
            </StackPanel>

            <!-- Line Items -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Bill Items" FontWeight="Bold" Margin="0,0,0,8" FontSize="13"/>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding Description}" materialDesign:HintAssist.Hint="Description" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
                                    <TextBox Grid.Column="1" Text="{Binding UnitPrice}" materialDesign:HintAssist.Hint="Price" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
                                    <TextBox Grid.Column="2" Text="{Binding Quantity}" materialDesign:HintAssist.Hint="Qty" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="3" Text="{Binding TotalPrice, StringFormat='{}{0:C}'}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0"/>
                                    <Button Grid.Column="4" Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Foreground="#EF4444">
                                        <materialDesign:PackIcon Kind="Delete" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Button Grid.Row="3" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding AddItemCommand}" HorizontalAlignment="Left" Margin="0,10">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0"/>
                    <TextBlock Text="ADD LINE ITEM" FontWeight="Bold"/>
                </StackPanel>
            </Button>

            <!-- Footer -->
            <Border Grid.Row="4" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0" Padding="0,20,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="TOTAL AMOUNT:" FontWeight="Black" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="16"/>
                        <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:C}'}" FontSize="24" FontWeight="Black" Foreground="#3B82F6" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="CANCEL" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Margin="0,0,15,0" Width="120" Height="45" materialDesign:ButtonAssist.CornerRadius="12"/>
                        <Button Style="{StaticResource MaterialDesignRaisedButton}" Content="SAVE INVOICE" Command="{Binding SaveInvoiceCommand}" Background="#3B82F6" BorderThickness="0" Width="150" Height="45" materialDesign:ButtonAssist.CornerRadius="12"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
