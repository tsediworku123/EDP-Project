<UserControl x:Class="HMS.Core.Views.LabTechHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="COMPLETED TEST HISTORY" FontSize="22" FontWeight="ExtraBold" Foreground="#0F172A" />
            <TextBlock Text="Search, filter, and review finalized laboratory reports." FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search and Filter Bar -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,25" Padding="20" Style="{StaticResource PremiumCard}" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="#F1F5F9" CornerRadius="8" Padding="15,0" Height="50" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" Foreground="#64748B" Width="24" Height="24" />
                        <TextBox Grid.Column="1" Margin="15,0,0,0" materialDesign:HintAssist.Hint="Search by Test Name, Category, or Technician..."
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 BorderThickness="0" FontSize="15" Background="Transparent" Foreground="#1E293B" VerticalAlignment="Center" />
                    </Grid>
                </Border>
                
                <Button Grid.Column="1" Style="{StaticResource MaterialDesignFlatButton}" Foreground="#3B82F6" 
                        Margin="15,0,0,0" Height="50" Padding="15,0" materialDesign:ButtonAssist.CornerRadius="8">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FilterVariant" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <TextBlock Text="ADVANCED FILTERS" FontWeight="Bold" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- History List -->
        <materialDesign:Card Grid.Row="2" Padding="0" Style="{StaticResource PremiumCard}" Background="White">
            <DataGrid ItemsSource="{Binding TestHistory}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      Background="Transparent" BorderThickness="0"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F5F9"
                      RowHeight="70" BorderBrush="Transparent" Margin="0">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White" />
                        <Setter Property="Foreground" Value="#94A3B8" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Padding" Value="20,15" />
                        <Setter Property="BorderThickness" Value="0,0,0,2" />
                        <Setter Property="BorderBrush" Value="#F1F5F9" />
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="20,0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type DataGridCell}">
                                    <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="TEST DETAILS" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TestName}" FontWeight="Bold" FontSize="15" Foreground="#1E293B" />
                                    <TextBlock Text="{Binding TestCategory}" FontSize="13" Foreground="#3B82F6" FontWeight="SemiBold" Margin="0,2,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="COMPLETED ON" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CompletedDate, StringFormat='{}{0:MMM dd, yyyy}'}" FontSize="14" Foreground="#334155" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding CompletedDate, StringFormat='{}{0:hh:mm tt}'}" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="PROCESSED BY" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="30" Height="30" CornerRadius="15" Background="#E2E8F0" Margin="0,0,10,0">
                                        <materialDesign:PackIcon Kind="AccountTieOutline" Width="18" Height="18" Foreground="#64748B" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <TextBlock Text="{Binding LabTechnicianName}" FontSize="14" Foreground="#1E293B" VerticalAlignment="Center" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="RESULT OUTCOME" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#F1F5F9" CornerRadius="6" Padding="12,6" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ResultSummary}" FontSize="13" FontWeight="Bold" Foreground="#475569" TextTrimming="CharacterEllipsis" MaxWidth="150" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTemplateColumn Header="ACTION" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource MaterialDesignOutlinedButton}" 
                                        BorderBrush="#E2E8F0" Foreground="#3B82F6" Background="White"
                                        Padding="15,0" Height="36" materialDesign:ButtonAssist.CornerRadius="6">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FileDocumentOutline" Width="18" Height="18" Margin="0,0,6,0" />
                                        <TextBlock Text="VIEW" FontWeight="Bold" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
