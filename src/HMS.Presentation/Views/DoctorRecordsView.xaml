<UserControl x:Class="HMS.Core.Views.DoctorRecordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="40">
        <StackPanel>
            <TextBlock Text="MEDICAL RECORDS" FontSize="24" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>
            <TextBlock Text="Manage and update patient medical history clinical notes." Opacity="0.6" Margin="0,0,0,30"/>
            <ItemsControl ItemsSource="{Binding Records}">
                 <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="15">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Date, StringFormat=dd MMM yyyy}" FontSize="12" Opacity="0.6"/>
                                <TextBlock Text="{Binding Description}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
