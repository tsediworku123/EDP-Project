<UserControl x:Class="HMS.Core.Views.AdminPatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="GLOBAL PATIENT DIRECTORY" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Margin="0,0,0,20" />

        <materialDesign:Card Grid.Row="1">
            <DataGrid ItemsSource="{Binding Patients}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Background="Transparent" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Registration No" Binding="{Binding PatientNumber}" Width="150" />
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*" />
                    <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="80" />
                    <DataGridTextColumn Header="Birth Date" Binding="{Binding DateOfBirth, StringFormat=dd MMM yyyy}" Width="150" />
                    <DataGridTemplateColumn Header="Action" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="RECORDS" Style="{DynamicResource MaterialDesignFlatButton}" Foreground="#3B82F6" Height="30" Padding="5,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
