<Window x:Class="HMS.Core.Views.AdminShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:HMS.Core.ViewModels"
        Title="ALPHA CLINIC | Administration" Height="850" Width="1350"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}">

    <Window.Resources>
        <Storyboard x:Key="ExpandSidebar">
            <DoubleAnimation Storyboard.TargetName="navColumn" Storyboard.TargetProperty="Width" To="240" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="CollapseSidebar">
            <DoubleAnimation Storyboard.TargetName="navColumn" Storyboard.TargetProperty="Width" To="70" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="navColumn" Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <materialDesign:Card Grid.Column="0" UniformCornerRadius="0">
            <materialDesign:Card.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="#0F172A" Offset="0"/>
                    <GradientStop Color="#1E293B" Offset="1"/>
                </LinearGradientBrush>
            </materialDesign:Card.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Margin="20,30" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}">
                    <TextBlock Text="ALPHA" Foreground="White" FontSize="32" FontWeight="ExtraBold" />
                    <TextBlock Text="ADMIN CONSOLE" Foreground="#94A3B8" FontSize="10" FontWeight="Bold" Margin="2,-4,0,0" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,10">
                    <Button Command="{Binding NavDashboardCommand}" Style="{StaticResource MaterialDesignNavButton}" Tag="ViewDashboard">
                        <TextBlock Text="DASHBOARD" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </Button>
                    <Button Command="{Binding NavDoctorsCommand}" Style="{StaticResource MaterialDesignNavButton}" Tag="Doctor">
                        <TextBlock Text="MANAGE DOCTORS" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </Button>
                    <Button Command="{Binding NavUsersCommand}" Style="{StaticResource MaterialDesignNavButton}" Tag="AccountGroup">
                        <TextBlock Text="USER ACCOUNTS" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </Button>
                    <Button Command="{Binding NavPatientsCommand}" Style="{StaticResource MaterialDesignNavButton}" Tag="Account">
                        <TextBlock Text="PATIENT LIST" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                    </Button>
                </StackPanel>

                <Button Grid.Row="2" Command="{Binding LogoutCommand}" Style="{StaticResource MaterialDesignNavButton}" Tag="Logout" Margin="10,0,10,30">
                    <TextBlock Text="LOGOUT" Foreground="#EF5350" FontWeight="Bold" Visibility="{Binding IsSidebarCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0" UniformCornerRadius="0" Background="White" MouseLeftButtonDown="DragWindow">
                <Grid Margin="40,0,20,0">
                    <TextBlock Text="{Binding ActivePageTitle}" Style="{StaticResource MaterialDesignHeadline6TextBlock}" FontWeight="ExtraBold" Foreground="#1E293B" VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="SYSTEM ADMINISTRATOR" Foreground="#1E293B" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,15,0" />
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Click="MinimizeWindow" Width="35" Height="35" Padding="0">
                            <materialDesign:PackIcon Kind="WindowMinimize" Foreground="#1E293B" Width="20" Height="20" />
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Click="MaximizeWindow" Width="35" Height="35" Padding="0">
                            <materialDesign:PackIcon Kind="WindowMaximize" Foreground="#1E293B" Width="20" Height="20" />
                        </Button>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Click="CloseWindow" Width="35" Height="35" Padding="0">
                            <materialDesign:PackIcon Kind="Close" Foreground="#EF5350" Width="20" Height="20" />
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" />
        </Grid>
    </Grid>
</Window>
